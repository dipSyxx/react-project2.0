import React from 'react'

export const Header = () => {
    const title = 'Regestration Form'

    return (
        <div>
            <h1 className='title'>{title}</h1>
        </div>
    )
}

export default Header;